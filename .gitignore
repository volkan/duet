# Top-level repo only versions docs (REQUIREMENTS, DECISION, SURVEY).
# duet/ is its own git repo — exclude it here so we don't double-track.
duet/

# OS / editor noise
.DS_Store
*.swp

# Future per-run outputs at the root level (none today)
runs/
