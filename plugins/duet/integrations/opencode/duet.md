---
description: Run and supervise the Duet two-agent CLI harness from OpenCode.
agent: build
---

Use the native `duet` skill through the skill tool. Forward the command's
arguments exactly as received (`$ARGUMENTS`) and follow the skill's launch,
monitoring, and stop procedures. Do not duplicate Duet launch logic here.
